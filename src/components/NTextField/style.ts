import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    minHeight: 24,
    fontSize: 12,
    lineHeight: 20,
    paddingHorizontal: 5,
    marginVertical: 5
  }
})
